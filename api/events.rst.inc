events
======

You can configure events to be ``POST``ed to the endpoints of your choice via the
dashboard. Once configured, events are also accessible via the events endpoint.


list all events
---------------

.. container:: method-description

  ``limit``
    *optional* integer. Defaults to ``10``.

  ``offset``
    *optional* integer. Defaults to ``0``.

.. container:: method-examples

