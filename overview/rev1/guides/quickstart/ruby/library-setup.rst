Add this line to your application's Gemfile:

.. code-block:: ruby

  gem 'balanced'

And then execute:

.. code-block:: ruby

  $ bundle

Or install it yourself via rubygems:

.. code-block:: ruby

  gem install balanced